# Regression test scope

The regression suite covers dependency ordering, missing dependency blocking, version incompatibility blocking, cycle blocking, transactional rollback of modified/new files and directories, and transaction commit preservation.
